---
- name: install basic services
  hosts: webservers
  become: true

  tasks:
    - name: install apache
      apt:
        name: apache2
        state: present

    - name: Start Apache
      service:
        name: apache2
        state: started


